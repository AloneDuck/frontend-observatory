# Architecture

The ingestion boundary normalizes untrusted browser observations into a shared v1 contract. Pure aggregation and budget modules keep release decisions deterministic. React only renders the resulting read model, so collector and policy behavior remain testable without a browser.
