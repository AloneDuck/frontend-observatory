# Contributing

Every metric change must include a contract fixture and a budget assertion. UI changes must keep keyboard navigation and automated accessibility checks green. Avoid recording URLs, query strings, or user identifiers in telemetry payloads.
